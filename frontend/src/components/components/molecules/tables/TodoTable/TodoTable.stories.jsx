import React from 'react';

import { TodosTable } from './TodosTable';

export default {
  title: 'Tables/TodosTable',
  component: TodosTable,
};

const Template = () => <TodosTable />;

export const Default = Template.bind({});
